# Extracted data — rebuildable from the API, never committed
data/

# Python
__pycache__/
*.py[cod]
.venv/
.ruff_cache/

# Node
node_modules/
.next/
out/

# Env
.env

# dbt
target/
dbt_packages/
logs/
.user.yml

# Generated export payloads (67MB, rebuildable)
web/public/data/

web/out/
# `next build` cannot delete web/out while OneDrive holds it, so a local build
# renames it aside first. Those leftovers are build output too.
web/out.old-*/
web/.next/
web/node_modules/

# TypeScript incremental build cache
*.tsbuildinfo

# Export payloads (~100MB, rebuildable, uploaded to object storage not git)
dist/

# Working notes: handoffs, ledgers, feature ideas. Local to whoever is working,
# never part of the repo.
thoughts/
